# install pipenv : It is a different pip only for this particular virtual environment
pip install pipenv

# install virtual env

pipenv install virtualenv

# to activate
pipenv shell



# in git.
git init  # to initialize the git repository
git add * # to add all the files to git
git status # check the status of the files
git commit -m 'message' # commiting the changes.


# to add to the github
git remote add origin git@github.com:tahsin-github/azure-text-analysis.git
git push -u origin master
